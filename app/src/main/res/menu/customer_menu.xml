<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_scan"
        android:icon="@drawable/ic_scan_symbol"
        android:title="@string/activity_scanner"
        app:showAsAction="always"
        />

    <item
        android:id="@+id/action_catalog"
        android:icon="@drawable/ic_menu_catalog_white"
        android:title="@string/catalogue"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/action_basket"
        android:icon="@drawable/ic_menu_basket_white"
        android:title="@string/basket_alt"
        app:showAsAction="always"
        />

</menu>